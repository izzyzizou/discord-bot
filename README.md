A basic discord bot that tracks user xp and current level.
Level up by simply sending messages in the channel.

Before running the app create a data directory with levels.json with following structure:
```json
{
  "123456789": {
    "xp": 40,
    "level": 1
  },
}
```
